Call the backtest API endpoints with defaults and verify:
- non-5xx status
- JSON structure includes winRate, profitFactor, sharpe, maxDrawdown
- constraints: market hours filter applied; lotSize respected
